# Root env — DB URL only (shared by packages/db migrations)
# All other secrets live in app-level .env files:
#   apps/api/.env         → local dev backend
#   apps/web/.env.local   → production (Hono inside Next.js)

DATABASE_URL=postgresql://user:pass@localhost:5432/seo_impact_os
