"use client"

// Stub for Phase 04 — Team Settings
// Wraps Better Auth organization plugin: listMembers, updateMemberRole, removeMember.
// Invite UI is visible but disabled — no email provider wired.
// Implementation: phase-04-team-settings.md

export function useTeamSettings() {
  throw new Error("useTeamSettings: not yet implemented (Phase 04)")
}
